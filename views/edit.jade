extends layout

block content
  .row
    .col-md-6
      h1 Edit an item
      form(role="form", method="post", action="/items/#{item.id}?_method=put")
        include editform
        button.btn.btn-primary(type="submit") Edit item
